{
  "name": "otherland",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "pnpm run build && pnpx gh-pages -d dist -t true"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "~5.6.2",
    "vite": "^5.4.9"
  },
  "dependencies": {
    "gh-pages": "^6.2.0",
    "three": "^0.169.0"
  }
}